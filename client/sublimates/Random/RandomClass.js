import RandomEngineClass from "@/sublimates/Random/RandomEngineClass";

export default class RandomClass extends RandomEngineClass{

    /*
    * Method for obtaining a random number in the range of received parameters,
    * minimum and maximum values. | number
    * */
    getRandomInt(min, max) {
        return super.getRandomInt(min, max);
    }


}